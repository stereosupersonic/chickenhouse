#sidebar
  .sidebar-module.sidebar-module-inset
    %h3.title.text-decoration-none Nächste Events
    - if Event.next_events.present?
      %ul#next-events
        - Event.next_events.limit(3).each do |event|
          %li.sidebar-event{ class: "event-#{event.id}" }
            = link_to event_path(event) do
              %i.fa.fa-calendar
              = "#{truncate(event.title, length: 21)} (#{time_ago_in_words event.start_date})".html_safe
    - else
      .sidebar-event
        = "Keine Events in der nächsten Zeit"
  .sidebar-module.sidebar-module-inset
    %h3
      %i.fa-brands.fa-instagram
      = link_to "Instagram", "https://www.instagram.com/henaheisl"

  .sidebar-module.sidebar-module-inset
    %h3
      %i.fa-brands.fa-facebook
      = link_to "Facebook", "http://www.facebook.com/henaheisl"
    .fb-like-box{"data-href" => "http://www.facebook.com/henaheisl",
                 "data-colorscheme" => "light",
                 "data-show-faces"  => "true",
                 "data-header"      => "false",
                 "data-stream"      => "false",
                 "data-show-border" => "false"}
