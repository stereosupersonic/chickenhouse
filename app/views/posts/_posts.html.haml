#posts
  .row
    - posts.each do | post |
      .col-12
        = render post

- if @pagy.pages > 1
  %nav{"aria-label" => "Seitennavigation"}
    .d-flex.justify-content-center.mt-4
      = raw pagy_bootstrap_nav @pagy
